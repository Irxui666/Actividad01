server:
  port: 8080
spring:
  datasource:
    url: jdbc:oracle:thin:@//localhost:1522/freepdb1
    username: ACTIVIDAD01
    password: 123456
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    properties:
      hibernate:
        format_sql: true

    database-platform: org.hibernate.dialect.OracleDialect
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    enabled: true